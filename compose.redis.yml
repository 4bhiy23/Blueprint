services:
  redis:
    build:
      context: ./docker/redis
    ports:
      - "127.0.0.1:6379:6379"
    volumes:
      - blueprint-redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

volumes:
  blueprint-redis-data:
